% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multilevel.R
\name{multilevel}
\alias{multilevel}
\title{Multilevel Decomposition for Multivariate Analysis}
\usage{
multilevel(
  x,
  y = NULL,
  design = NULL,
  ncomp = NULL,
  algo = c("nipals", "svd")[1],
  scaling = c("none", "center", "standard")[2],
  scalingY = c("none", "center", "standard")[2],
  cvFolds = 10,
  rep = 10,
  perm = 20,
  train = 1,
  alpha = 0.05,
  parallel = FALSE,
  method = c("pca", "pls", "plsda")[1]
)
}
\arguments{
\item{x}{A matrix, data.frame}

\item{y}{Matrix or data.frame containing the response variables.}

\item{design}{A named vector identifying the groups/subjects for decomposition.}

\item{ncomp}{Number of components. If \code{NULL}, it is estimated based on design groups.}

\item{algo}{Character. Algorithm to use: "nipals", or "svd" (Single Value Decomposition).}

\item{scaling}{Character. Scaling method: "none", "center" (default), "standard", "softBlock", or "hardBlock".}

\item{scalingY}{Scaling method for Y: "none", "center" (default), or "standard".}

\item{cvFolds}{Number of cross-validation folds. Default is 10.}

\item{rep}{Number of cross-validation repetitions. Default is 10.}

\item{perm}{Number of permutations for model validation. Default is 20.}

\item{train}{Training set percentage (0 to 1). Default is 1 (all data).}

\item{alpha}{Significance level for Jack-knife confidence intervals. Default is 0.05.}

\item{parallel}{Logical. If \code{TRUE}, cross-validation runs in parallel.}

\item{method}{Character. The multivariate method to use: "pca", "pls", or "plsda".}
}
\value{
A list containing the decomposed matrices (Xm, Xw, Xb) and the
resulting multivariate model. On pca both the models of the within and between parts are returned.
}
\description{
Performs a multilevel decomposition of a data matrix into within-subject and
between-subject variations. This is particularly useful for crossover or
repeated measures designs where the individual "signature" needs to be
separated from the treatment effect.
}
