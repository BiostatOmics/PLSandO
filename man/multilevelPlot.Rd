% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multilevel.R
\name{multilevelPlot}
\alias{multilevelPlot}
\title{Plotting Multilevel Multivariate Models}
\usage{
multilevelPlot(
  x,
  type = c("scree", "scores", "R2vsQ2", "loadings", "scoresX", "loadingsX", "scoresY",
    "loadingsY", "weights", "linearity", "overfitting", "R2", "corr", "biplot", "coef",
    "ncomp"),
  comp = NULL,
  col = c("main", "complete", "cblindfriendly", "sunshine", "hot", "warm", "grass",
    "oficial")[1],
  colBy = NULL,
  shape = NULL,
  shapeBy = NULL,
  ellipses = NULL,
  selVars = NULL,
  labels = NULL,
  labelTop = NULL,
  repel = TRUE,
  newObs = NULL,
  newdesign = NULL
)
}
\arguments{
\item{x}{An object returned by the \code{multilevel} function.}

\item{type}{Character. The type of plot (e.g., "scores", "loadings", "biplot", "scree").}

\item{comp}{Numeric vector. Components to plot (e.g., \code{c(1,2)}). By default, components 1 and 2 are used.}

\item{col}{Character. A predefined color palette name (e.g., "main", "oficial", ...)  or a vector of custom colors.}

\item{colBy}{Variable used to color the points.
\itemize{
  \item For \strong{Scores}: A column name from the dataset or an external vector.
  \item For \strong{Loadings/Correlation}: Can be one of: \code{"contrib"} (to color by variable contribution)
  or \code{"cos2"} (to color by the quality of representation).
  \item For \strong{biPlot}: A column name from the dataset or an external vector to color the observations.
}
By default, no variable is used.}

\item{shape}{Numeric or character. The shape of the points (numeric) in the Score plots or 'arrow', 'point' for loading plots.}

\item{shapeBy}{Variable used to change point shapes. Can be a column name from the original dataset or an external vector/factor. Must be categorical.}

\item{ellipses}{Logical. If \code{TRUE}, draws 95\% confidence ellipses for groups defined in \code{colBy}.}

\item{selVars}{Numeric. The number of top variables to display in loading, correlation, or biplots, selected by their importance ("contrib" or "cos2"). Useful for decluttering plots with many variables.}

\item{labels}{Logical or Character vector. If \code{TRUE}, uses row names as labels. If Character, uses the provided vector as labels.}

\item{labelTop}{Numeric (0 to 1). Percentage of variables to label based on their importance (contribution or cos2).}

\item{repel}{Logical. If \code{TRUE}, uses \code{ggrepel} to prevent label overlap (recommended for loadings and biplots).}

\item{newObs}{Optional. A data frame (or list of data frames for MB-PCA) containing new observations to project onto the existing model.}

\item{newdesign}{A named vector identifying the groups/subjects for the new observations.}
}
\value{
A ggplot object (or patchwork of plots).
}
\description{
Visualizes the results of a multilevel analysis.
}
